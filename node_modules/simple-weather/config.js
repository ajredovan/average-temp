"use strict";

var _ = require("lodash");

var previousConfig;

module.exports = function (userConfig) {
    if (previousConfig) {
        return previousConfig;
    }


    previousConfig = _.extend(
        {
            apiKey: 'ccaf5561c047dce8ddeb08001b5d8b8e'
        },
        userConfig,
        {
            api: {
                "v2.5": {
                    url: "api.openweathermap.org/data/2.5"
                }
            }
        });

    return previousConfig;
};
